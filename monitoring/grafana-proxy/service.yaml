apiVersion: v1
kind: Service
metadata:
  name: grafana-proxy
  namespace: default
  labels:
    app: grafana-proxy
spec:
  type: ClusterIP
  selector:
    app: grafana-proxy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
