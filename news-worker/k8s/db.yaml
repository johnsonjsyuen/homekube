apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: news-db
  namespace: temporal
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: news
      owner: news
