apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: news-worker
  namespace: temporal
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: news-worker
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
