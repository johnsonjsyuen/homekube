apiVersion: v1
kind: Service
metadata:
  name: news-worker
  namespace: temporal
  labels:
    app: news-worker
spec:
  selector:
    app: news-worker
  ports:
  - name: http
    port: 80
    targetPort: 3000
