apiVersion: v1
kind: Service
metadata:
  name: text-to-speech
  namespace: default
  labels:
    app: text-to-speech
spec:
  selector:
    app: text-to-speech
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
