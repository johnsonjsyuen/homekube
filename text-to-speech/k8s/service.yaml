apiVersion: v1
kind: Service
metadata:
  name: text-to-speech
  namespace: default
  labels:
    app: tts-api
spec:
  selector:
    app: tts-api
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
