apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: text-to-speech
  namespace: default
spec:
  selector:
    matchLabels:
      app: text-to-speech
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
