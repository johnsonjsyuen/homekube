apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tts-models
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce  # Single-node cluster; mounted readOnly by pods for ROX semantics
  resources:
    requests:
      storage: 2Gi
