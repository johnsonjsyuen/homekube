apiVersion: v1
kind: Service
metadata:
  name: location-share-server
  namespace: default
  labels:
    app: location-share-server
spec:
  selector:
    app: location-share-server
  ports:
  - name: http
    port: 80
    targetPort: 3000
