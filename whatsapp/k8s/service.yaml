apiVersion: v1
kind: Service
metadata:
  name: whatsapp
  namespace: default
  labels:
    app: whatsapp
spec:
  selector:
    app: whatsapp
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000
  type: ClusterIP
