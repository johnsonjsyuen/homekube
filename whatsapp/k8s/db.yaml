apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: whatsapp-db
  namespace: default
spec:
  instances: 2
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: whatsapp
      owner: app
  monitoring:
    enablePodMonitor: true
