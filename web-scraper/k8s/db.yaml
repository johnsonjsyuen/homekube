apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: web-scraper-db
  namespace: temporal
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: web_scraper
      owner: web_scraper
